# Copie este arquivo para .env e preencha com seus valores reais
# NUNCA commite o arquivo .env de verdade no git

# Token do bot do Telegram (crie um via @BotFather)
TELEGRAM_BOT_TOKEN=

# ID do chat/canal do Telegram para onde as vagas serão postadas
TELEGRAM_CHAT_ID=

# Palavra-chave de busca (ex: "estagio desenvolvimento Curitiba")
SEARCH_KEYWORD=estagio desenvolvimento

# Intervalo entre execuções do scraper, em minutos
SCRAPE_INTERVAL_MINUTES=90

# URL do serviço api-vagas (Meu Padrinho), rodado à parte (Docker/node).
# Local: http://localhost:3001 | Deploy: URL pública do serviço.
JOBS_API_BASE_URL=http://localhost:3001

# Máximo de vagas enviadas por ciclo (anti-flood).
MAX_VAGAS_POR_CICLO=8

# Slots do cap reservados só para fontes BR (api-vagas/Gupy/Indeed).
RESERVA_VAGAS_BR=3
